[package]
name = "user/package"
version = "0.0.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

exclude = ["*"]
include = ["src", "src/*", "default.project.json", "wally.toml"]

[dependencies]